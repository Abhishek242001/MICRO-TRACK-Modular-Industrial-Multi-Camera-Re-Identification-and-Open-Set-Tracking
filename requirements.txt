# MICRO-TRACK — requirements
# Tested on Python 3.10 / 3.11 / 3.12

# Core computer vision
ultralytics>=8.0.0          # YOLOv8 detection
opencv-python>=4.8.0        # frame capture, crop, resize, display
numpy>=1.24.0               # array ops

# ReID model inference
onnxruntime>=1.16.0         # CPU inference (auto-used if no GPU)
# onnxruntime-gpu>=1.16.0   # uncomment for CUDA acceleration

# ONNX export (only needed to run build_reid_onnx.py)
torch>=2.0.0
torchvision>=0.15.0
onnx>=1.14.0
onnxscript>=0.1.0
